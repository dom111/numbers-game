/**
 * @purpose Bootstraps the `Game` component into the page.
 * @inputs Optional startup configuration supplied via host element attributes.
 * @outputs None directly; interactive behavior is delegated to `Game`.
 * @invariants App startup should mount exactly one game root per host container.
 */

import './styles.css';
import './components/target.js';
import './components/number.js';
import './components/operators.js';
import './components/step.js';
import './components/numbers.js';
import './components/steps.js';
import './components/game.js';
